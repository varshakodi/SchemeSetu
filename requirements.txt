# Pinned exact versions (PRD §8: an unpinned repo that stops running is a dead
# portfolio piece). After installing/upgrading anything, update the pin here.
numpy==2.4.6
sentence-transformers==6.0.0
anthropic==1.0.0
pypdf==6.16.2
pytest==9.1.1
