# RUNTIME dependencies — what the deployed app needs. Kept minimal so the
# hosted build stays small and fast. Dev/eval extras: requirements-dev.txt.
#
# The CPU-only PyTorch index matters for deployment: on Linux the default
# `torch` wheel bundles CUDA (~2.5 GB) and will blow a free host's build
# budget. This project runs inference on CPU everywhere.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy==2.4.6
torch==2.13.0
sentence-transformers==6.0.0
langgraph==1.2.11
streamlit==1.62.0
httpx==0.28.1
