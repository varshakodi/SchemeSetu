# virtual environment & caches
.venv/
__pycache__/
*.pyc
.DS_Store

# secrets — never commit API keys
.env

# the real corpus (large; documented in the data registry instead)
data/raw/

# The search index IS committed: it is small (~0.6 MB), and the deployed app
# needs it to run without rebuilding (and without shipping the corpus).
# Rebuild after any corpus change:  python naive/rag.py index

.streamlit/secrets.toml

# personal study notes — kept on disk, never published
private/
